name: quotex_algorithm_v3
description: Quotex Algorithm Bot V3.1 - RSI + MACD Signals
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  provider: ^6.0.5
  flutter_local_notifications: ^16.1.0
  charts_flutter: ^0.12.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
